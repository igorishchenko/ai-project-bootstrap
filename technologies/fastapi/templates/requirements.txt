# Python dependencies for {{projectName}}.
#
# Pinned on purpose: an environment that only exists on one machine is not
# reproducible, and CI will not match it. Regenerate with `pip freeze`.
fastapi[standard]>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6

# Development
pytest>=8.3
httpx>=0.27
ruff>=0.7
