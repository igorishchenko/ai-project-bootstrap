/**
 * Stylesheet stub for Jest.
 *
 * Bundlers turn `import './Button.css'` into something meaningful; Jest does
 * not, and would try to parse the CSS as JavaScript. Mapping style imports here
 * makes them a no-op.
 *
 * Only referenced by the web Jest configuration — harmless otherwise.
 */
module.exports = {};
