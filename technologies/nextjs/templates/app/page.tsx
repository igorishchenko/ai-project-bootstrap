export default function Home(): React.JSX.Element {
  return (
    <main>
      <h1>{{projectName}}</h1>
      <p>This page is a placeholder so the project runs on day one. Replace it.</p>
      <p>
        Start with <code>docs/setup.md</code> — it carries the setup steps for every technology in
        this stack, in the order they need doing.
      </p>
    </main>
  );
}
