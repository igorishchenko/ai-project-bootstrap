import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Deliberately empty. Reach for a config option when something actually needs
  // it — every entry here is a build behaviour someone has to know about later.
  // `npm run typecheck` and `npm run lint` run separately; Next 16 no longer
  // runs either during `next build`.
};

export default nextConfig;
