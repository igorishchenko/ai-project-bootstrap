# well-documented-fixture

A synthetic fixture repo used by `tests/analyzeChecks.test.ts` and
`tests/analyze.test.ts` to exercise `ai-project-bootstrap analyze` against a
repo with every signal present, so the tests assert against a known-good
baseline rather than guessing at real-world repo state.
